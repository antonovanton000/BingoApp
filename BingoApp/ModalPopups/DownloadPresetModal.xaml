<UserControl x:Class="BingoApp.ModalPopups.DownloadPresetModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BingoApp.ModalPopups"
             xmlns:vm="clr-namespace:BingoApp.ViewModels"
             xmlns:cnt="clr-namespace:BingoApp.Controls"
             mc:Ignorable="d" 
             x:Name="page"
             d:DesignHeight="650" d:DesignWidth="1200">
    <Border BorderBrush="{StaticResource BorderColorBrush}" Background="{StaticResource BackgroundBrush}"
            Height="240" Width="340" BorderThickness="1" CornerRadius="12">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <cnt:SpinerControl  SpinerColor="{StaticResource AccentBrush}" Margin="0,0,0,20"/>
            <TextBlock Style="{StaticResource FlatTextBlockStyle}" Text="{DynamicResource mes_downloading }"/>
        </StackPanel>
    </Border>
</UserControl>
