<Page x:Class="HuntpointApp.Views.ConnectToRoomPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:cnt="clr-namespace:HuntpointApp.Controls"
      xmlns:local="clr-namespace:HuntpointApp.Views"
      xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:vm="clr-namespace:HuntpointApp.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="1440" d:DesignWidth="670"
      Title="NewRoomPage"
      x:Name="page">
    <Page.DataContext>
        <vm:ConnectToRoomViewModel />
    </Page.DataContext>
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded" SourceObject="{Binding ElementName=page}">
            <behaviors:InvokeCommandAction Command="{Binding AppearingCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>
    <Page.Resources>
        <Storyboard x:Key="WelcomeAnimation">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.Opacity)">
                <EasingDoubleKeyFrame KeyTime="00:00:00" Value="0"/>
                <EasingDoubleKeyFrame KeyTime="00:00:00.3000000" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.X)">
                <EasingDoubleKeyFrame KeyTime="00:00:00.0" Value="100"/>
                <EasingDoubleKeyFrame KeyTime="00:00:00.3000000" Value="1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </Page.Resources>
    <Page.Triggers>
        <EventTrigger RoutedEvent="Page.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WelcomeAnimation}"/>
        </EventTrigger>
    </Page.Triggers>
    <Border x:Name="border" Margin="0,0,0,0" Padding="40,0,40,40" VerticalAlignment="Stretch" Background="#030406" RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TransformGroup>
                <ScaleTransform/>
                <SkewTransform/>
                <RotateTransform/>
                <TranslateTransform/>
            </TransformGroup>
        </Border.RenderTransform>
        <Grid x:Name="gr_connectRoom" Margin="0,0,0,0">
            <Grid Margin="0,40,0,0" Visibility="{Binding IsConnectingModalVisible, Converter={StaticResource NotBoolToVisibilityConverter}}" d:Visibility="Collapsed">
                <Grid VerticalAlignment="Top" Margin="0,0,0,0">
                    <Image Source="/Images/tl_corner.png" Height="80" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0.2" />
                    <Image Source="/Images/tr_corner.png" Height="80" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2"/>
                    <TextBlock Text="{DynamicResource mp_connect}" Style="{StaticResource SubHeaderTextBlockBlockStyle2}"
                           FontFamily="{StaticResource Manege}" HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="35"/>
                </Grid>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="1*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Margin="0,90,0,0">
                        <TextBlock Text="{DynamicResource mes_connectroomtext}" Style="{StaticResource FlatTextBlockStyle}"
                                   FontFamily="{StaticResource Manege}" Margin="0,0,0,5" FontSize="19" HorizontalAlignment="Left"/>

                        <cnt:InputFieldControl Text="{Binding RoomId, Mode=TwoWay}" Placeholder="{DynamicResource mp_roomid}" Label="{DynamicResource mp_roomid}" Margin="0,0,0,10"
                         IsError="{Binding IsRoomIdError}" ErrorMessage="{DynamicResource mp_requieredfield}" IsRequiered="True"/>

                        <Button Style="{StaticResource FlatButtonStylePrimaryNoArrow}" 
                            Command="{Binding GetRoomConnectionInfoCommand}"
                            Content="{DynamicResource mp_connect}" Width="250" HorizontalAlignment="Center" Margin="0,30,0,20"/>
                    </StackPanel>
                    <TextBlock Text="{DynamicResource mp_activerooms}" Style="{StaticResource SubHeaderTextBlockBlockStyle2}" Grid.Row="1"
                           FontFamily="{StaticResource Manege}" HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="35"
                           Visibility="{Binding ActiveRooms.Count, Converter={StaticResource CountToVisibilityConverter}}"/>
                    <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" Margin="0,5,0,50" VerticalScrollBarVisibility="Auto"
                              Visibility="{Binding ActiveRooms.Count, Converter={StaticResource CountToVisibilityConverter}}">
                        <ItemsControl ItemsSource="{Binding ActiveRooms}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border x:Name="activeBoardBorder" BorderThickness="0,0,0,0" CornerRadius="5" Margin="0,0,0,8" Background="#05FFFFFF" BorderBrush="{StaticResource BorderColorBrush}">
                                        <behaviors:Interaction.Triggers>
                                            <behaviors:EventTrigger EventName="MouseLeftButtonDown" SourceObject="{Binding ElementName=activeBoardBorder}">
                                                <behaviors:InvokeCommandAction Command="{Binding DataContext.ContinueRoomCommand,ElementName=page}" 
                                           CommandParameter="{Binding }"/>
                                            </behaviors:EventTrigger>
                                        </behaviors:Interaction.Triggers>
                                        <Border.Triggers>
                                            <EventTrigger RoutedEvent="MouseEnter">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="hoverbg" Storyboard.TargetProperty="Opacity"
                                     To="1" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                            <EventTrigger RoutedEvent="MouseLeave">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="hoverbg" Storyboard.TargetProperty="Opacity"
                                     To="0" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Border.Triggers>
                                        <Grid HorizontalAlignment="Stretch" Height="70">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="1*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border x:Name="hoverbg" CornerRadius="5" Opacity="0" Grid.ColumnSpan="2" Background="#10FFFFFF"/>
                                            <Grid>
                                                <!--<Image Source="/Images/bingocolored.png" Margin="10"/>-->
                                            </Grid>
                                            <StackPanel Grid.Column="1" Margin="15,10,3,0">
                                                <TextBlock Text="{Binding RoomName}" Style="{StaticResource FlatTextBlockStyle}" 
                                                       VerticalAlignment="Center" FontSize="18" Foreground="#90FEFEFE" TextTrimming="CharacterEllipsis"
                                                       Margin="0,0,60,0"/>
                                                <TextBlock Text="{Binding CreationDate, StringFormat='dd/MM/yyyy HH:mm'}" Style="{StaticResource FlatTextBlockStyle}" 
                                                       VerticalAlignment="Center" FontSize="13" Foreground="#30FEFEFE" Margin="0,5,0,0"/>
                                            </StackPanel>
                                            <Button Command="{Binding DataContext.EndGameCommand,ElementName=page}"
                                                CommandParameter="{Binding }"
                                                Style="{StaticResource IconButtonStyleColored}" 
                                                Content="&#xe7C1;" HorizontalAlignment="Right"
                                                        Foreground="{StaticResource Green}"
                                                        Background="{StaticResource GreenAlpha20}"
                                                        ToolTip="{DynamicResource rp_finishgame}"
                                                        Grid.Column="2" Margin="0,0,10,0"
                                                />
                                            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Orientation="Horizontal" Visibility="Collapsed">
                                                <Button Command="{Binding DataContext.ContinueRoomCommand,ElementName=page}"                
                                                    Style="{StaticResource IconButtonStyleColored}" 
                                                    Content="&#xe102;" HorizontalAlignment="Right"
                                                            Foreground="{StaticResource Green}"
                                                            Background="{StaticResource GreenAlpha20}"                                                        
                                                            Margin="0,0,10,0" ToolTip="Continue Game"
                                                    />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Vertical"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                    <Button Grid.Row="2" Style="{StaticResource FlatTextButtonStyleNoArrow}" VerticalAlignment="Bottom"
                            Content="{DynamicResource mp_cancel}" Command="{Binding ClosePageCommand}" Width="150"/>
                </Grid>
            </Grid>
            <Grid Margin="0,40,0,0" Visibility="{Binding IsConnectingModalVisible, Converter={StaticResource BoolToVisConverter}}" d:Visibility="Visible">
                <Grid VerticalAlignment="Top" Margin="0,0,0,0">
                    <Image Source="/Images/tl_corner.png" Height="80" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0.2" />
                    <Image Source="/Images/tr_corner.png" Height="80" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2"/>
                    <TextBlock Text="{DynamicResource mp_connecttotheroo}" Style="{StaticResource SubHeaderTextBlockBlockStyle2}"
                           FontFamily="{StaticResource Manege}" HorizontalAlignment="Center" Margin="0,20,0,0" FontSize="35"/>
                </Grid>
                <Grid Margin="0,70,0,0">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid>
                            <StackPanel Visibility="{Binding IsConnectingToBoardStarted, Converter={StaticResource NotBoolToVisibilityConverter}}">
                                <cnt:InputFieldControl IsReadOnly="True" Text="{Binding RoomInfo.RoomName}" Placeholder="{DynamicResource mp_roomname}" Label="{DynamicResource mp_roomname}" Margin="40,0,40,10"/>
                                <cnt:InputFieldControl IsReadOnly="True" Text="{Binding RoomInfo.CreatorName}" Placeholder="{DynamicResource mp_creator}" Label="{DynamicResource mp_creator}" Margin="40,0,40,10"/>

                                <cnt:InputFieldControl IsReadOnly="True" Text="{Binding RoomInfo.GameAndPreset}" Placeholder="{DynamicResource mp_game}" Label="{DynamicResource mp_game}" Margin="40,0,40,10" />
                                <cnt:InputFieldControl IsReadOnly="True" Text="{Binding RoomInfo.LegendaryObjective}" Placeholder="{DynamicResource mp_legendaryobjective}" Label="{DynamicResource mp_legendaryobjective}" Margin="40,0,40,10" />
                                

                                <cnt:InputFieldControl Text="{Binding Credentials.NickName, Mode=TwoWay}" Placeholder="{DynamicResource mp_nickname}" Label="{DynamicResource mp_nickname}" Margin="40,0,40,10"/>
                                <TextBlock Visibility="{Binding IsEmptyNickName, Converter={StaticResource BoolToVisConverter}}" Text="{DynamicResource mp_pleasetypenickn}" Style="{StaticResource FlatTextBlockStyle}" Foreground="{StaticResource Danger}" Margin="55,-5,0,-15"/>
                                <cnt:InputFieldControl IsPassword="True" Password="{Binding Credentials.Password, Mode=TwoWay}" Placeholder="{DynamicResource mp_password}" Label="{DynamicResource mp_password}" Margin="40,0,40,50"/>
                                <TextBlock Visibility="{Binding IsIncorrectPassword, Converter={StaticResource BoolToVisConverter}}" Text="{DynamicResource mp_incorrectpasswo}" Style="{StaticResource FlatTextBlockStyle}" Foreground="{StaticResource Danger}" Margin="55,-45,0,0"/>
                                <cnt:SwitchControl Text="{DynamicResource mp_forfirstdeath}" IsChecked="{Binding RoomInfo.IsForFirstDeath, Mode=TwoWay}" Margin="0,0,0,20"
                                           Visibility="{Binding RoomInfo.IsSekiro, Converter={StaticResource BoolToVisConverter}}" d:Visibility="Visible"/>
                                <cnt:SwitchControl Text="{DynamicResource mp_generatesameseed}" IsChecked="{Binding RoomInfo.IsGenerateSameSeeds, Mode=TwoWay}" Margin="0,0,0,20"
                                            Visibility="{Binding RoomInfo.IsNightreign, Converter={StaticResource BoolToVisConverter}}" d:Visibility="Visible"/>
                                <cnt:SwitchControl Text="{DynamicResource mp_extrapointsforlegendary}" Margin="45,-15,0,30" IsChecked="{Binding RoomInfo.IsLegendaryExtraPoints, Mode=TwoWay}" IsEnabled="False"/>
                                <cnt:SwitchControl Text="{DynamicResource mp_connectasspecta}" Margin="45,-15,0,30" IsChecked="{Binding Credentials.IsSpectator, Mode=TwoWay}"/>
                                <Button Style="{StaticResource FlatButtonStylePrimary}" Command="{Binding ConnectToRoomCommand}" Content="{DynamicResource mp_connect}" Width="250" HorizontalAlignment="Center"/>
                                <Button Content="{DynamicResource mp_cancel}" Style="{StaticResource FlatTextButtonStyleNoArrow}" 
                                    Command="{Binding ConnectingToRoomCancelCommand}" Margin="0,40,0,0"
                                    Width="180"/>
                            </StackPanel>
                            <Grid Visibility="{Binding IsConnectingToBoardStarted, Converter={StaticResource BoolToVisConverter}}">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding RoomInfo.RoomName, StringFormat={StaticResource mp_connectingto0}}" Style="{StaticResource FlatTextBlockStyle}"
                                           Margin="0,0,0,50" FontSize="25" HorizontalAlignment="Center" TextAlignment="Center"/>

                                    <Button Content="{DynamicResource mp_cancel}" Style="{StaticResource FlatButtonStyle}" 
                                        Command="{Binding ConnectToRoomCancelCommand}" Margin="0,40,0,0"
                                        Width="180"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Grid>

        </Grid>
    </Border>
</Page>
