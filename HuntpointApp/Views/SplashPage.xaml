<Page x:Class="HuntpointApp.Views.SplashPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:HuntpointApp.Views"
      xmlns:cnt="clr-namespace:HuntpointApp.Controls"
      xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="700"
      xmlns:vm="clr-namespace:HuntpointApp.ViewModels"
      x:Name="page"
      Title="SplashPage">
    <Page.DataContext>
        <vm:SplashViewModel />
    </Page.DataContext>
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded" SourceObject="{Binding ElementName=page}">
            <behaviors:InvokeCommandAction Command="{Binding AppearingCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>
    <Page.Resources>

    </Page.Resources>
    <Grid>
        <Border Height="520" Width="520" Background="{StaticResource GradientBackgroundBrush}" CornerRadius="310" Margin="40" x:Name="mainBorded" RenderTransformOrigin="0.5 0.5">
            <Border.Effect>
                <DropShadowEffect Color="#3D71D9" Opacity="0.1" BlurRadius="20" ShadowDepth="0"/>
            </Border.Effect>
            <Border ClipToBounds="True">
                <Border.Clip>
                    <RectangleGeometry Rect="0 0 520 520" RadiusX="310" RadiusY="310"/>
                </Border.Clip>
                <Grid>
                    <Image Source="/Images/mainimg.jpg"  Height="520" Stretch="UniformToFill" Opacity="0.08" Margin="-100,0,0,0"/>
                    <TextBlock Text="HuntpointApp" HorizontalAlignment="Center" VerticalAlignment="Center"
                               Foreground="White" FontSize="45" FontFamily="Segoe UI" Margin="0,0,0,0"/>

                    <cnt:SpinerControl SpinerColor="{StaticResource AccentBrush}" VerticalAlignment="Bottom" Margin="0,0,0,80" IsSmall="True" />
                    <TextBlock Foreground="{StaticResource PrimaryTextBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,40">                
                        <Run Text="{Binding Version}"/>
                        <Run Text="(beta)"/>
                    </TextBlock>

                </Grid>
            </Border>
        </Border>
    </Grid>
</Page>
