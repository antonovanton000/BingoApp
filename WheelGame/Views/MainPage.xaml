<Page x:Class="WheelGame.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:vm="clr-namespace:WheelGame.ViewModels"
      xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:local="clr-namespace:WheelGame.Views"
      xmlns:cnt="clr-namespace:WheelGame.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage" x:Name="page">
    <Page.DataContext>
        <vm:MainViewModel/>
    </Page.DataContext>
    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded" SourceObject="{Binding ElementName=page}">
            <behaviors:InvokeCommandAction Command="{Binding AppearingCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Width="180" VerticalAlignment="Center">
            <Button Content="Создать комнату" Command="{Binding CreateRoomCommand}" Style="{StaticResource FlatButtonStylePrimaryNoArrow}" Margin="0,0,0,20"/>
            <Button Content="Подключиться к комнате" Command="{Binding ConnectRoomCommand}" Style="{StaticResource FlatButtonStyle}"/>
        </StackPanel>
        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="450">
            <TextBlock Text="Настройки" Style="{StaticResource FlatTextBlockStyle}"/>
            <cnt:InputFieldControl Label="Никнейм" Placeholder="Никнейм" Text="{Binding Nickname, Mode=TwoWay}"/>
            <cnt:InputFieldControl Label="Время таймера (с)" Placeholder="Время таймера (с)" IsNumberOnly="True" Text="{Binding TimerSeconds, Mode=TwoWay}"/>
            <Button Style="{StaticResource FlatButtonStylePrimary}" Content="Сохранить" Command="{Binding SaveSettingsCommand}" Width="140" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>
